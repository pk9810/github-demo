name : first

on : push

jobs:
        jobs1:
                name: first job
                runs-on: ubuntu-latest
                steps:
                        - name: Step One
                          uses: actions/checkout@v2
                        - name: step two 
                          run: env | sort 
        jobs2:
                name: Second job
                runs-on: ubuntu-latest
                steps:
                        -name: Step one
                         uses: actions/checkout@v2
                        -name: Step two 
                         run: "Get-ChildItem Env: | Sort-Object Name"

